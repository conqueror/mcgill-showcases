# Learning Guide

1. Run `make run` and compare encoding choices.
2. Open feature selection scores and inspect top-ranked features.
3. Run `make run-dimred` and inspect embedding quality metrics.
4. Compare visualization clarity between PCA and t-SNE.

