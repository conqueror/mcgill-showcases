# Learning Guide

1. Run `make run` and inspect `artifacts/eda/*` outputs.
2. Compare split strategies in `artifacts/splits/*manifest.json`.
3. Review leakage findings and discuss mitigation actions.
4. Enable profiling extras and compare artifacts.
