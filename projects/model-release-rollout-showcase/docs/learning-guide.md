# Learning Guide

1. Run `make run` and inspect canary metrics.
2. Compare decision threshold inputs in `decision_log.json`.
3. Edit thresholds in `scripts/run_rollout.py` and rerun.
4. Discuss when rollback is preferable over hold.
