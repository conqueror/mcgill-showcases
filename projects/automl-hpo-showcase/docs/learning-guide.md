# Learning Guide

1. Run `make run` and inspect `strategy_comparison.csv`.
2. Find best configs in `best_configs.json`.
3. Run `make run-budget` and inspect cost-vs-score curves.
4. Explain where each strategy becomes preferable.

