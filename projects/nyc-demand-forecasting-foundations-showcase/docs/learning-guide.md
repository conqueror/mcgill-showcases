# Learning Guide

1. Run `make run` and inspect `artifacts/splits/time_split_manifest.json`.
2. Explain why time-ordered splitting is required for demand forecasting.
3. Compare MAE, RMSE, and sMAPE in `artifacts/eval/metrics_summary.csv`.
4. Review false-high and false-low predictions in `artifacts/eval/prediction_examples.csv`.
