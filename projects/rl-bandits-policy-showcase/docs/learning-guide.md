# Learning Guide

1. Run `make run` to generate reward and regret traces.
2. Compare curves by strategy.
3. Run `make run-compare` and inspect final ranking.
4. Explain why top policy wins under the chosen horizon.

