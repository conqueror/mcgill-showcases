# Learning Guide

1. Run `make run-compare` to generate events and both pipelines.
2. Inspect parity differences by window.
3. Change allowed lateness and rerun stream mode.
4. Discuss tradeoffs between latency and completeness.

