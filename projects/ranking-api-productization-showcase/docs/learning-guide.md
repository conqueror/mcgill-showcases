# Learning Guide

1. Run `make train-demo` to generate a model artifact bundle.
2. Use `make test` to validate endpoint behavior and error handling.
3. Start the service with `make dev` and inspect `/docs`.
4. Export and inspect `openapi.json` to discuss contract-first API workflows.
